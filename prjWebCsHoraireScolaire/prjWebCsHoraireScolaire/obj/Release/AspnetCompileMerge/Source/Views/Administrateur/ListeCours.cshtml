@model IEnumerable<prjWebCsHoraireScolaire.Models.Cour>

@{
    ViewBag.Title = "ListeCours";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>ListeCours</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.CoursID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoursID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoursID })*@
        </td>
    </tr>
}

</table>
